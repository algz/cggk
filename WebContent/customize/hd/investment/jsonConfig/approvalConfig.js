
// 送审配置对象
approvalConfig = {
	// 业务对象类型1
	'bizObjectType1' = { 
	    templateId = '', // 审批流程模板
	    approvalNote = '', // 送审附言
	    notFilter = '' // 是否不判断权限，true表示不判断，false表示判断
	},
	
	// 业务对象类型2
	'bizObjectType2' = { 
	    templateId = '', // 审批流程模板
	    approvalNote = '', // 送审附言
	    notFilter = '' // 是否不判断权限，true表示不判断，false表示判断
	}
};